driver:
  name: docker
  use_sudo: false
  privileged: true

provisioner:
  name: salt_solo
  salt_install: none
  formula: k0s

platforms:
  - name: ubuntu
    driver_config:
      image: salt-kitchen:ubuntu-22.04
      run_command: /sbin/init
      run_options: --cgroupns=host
      volume:
        - /sys/fs/cgroup:/sys/fs/cgroup:rw

verifier:
  name: shell

suites:
  - name: default
    verifier:
      command: .venv/bin/pytest tests/integration/default/ -v --html=reports/report-default.html
    provisioner:
      pillars: {}
      pillars_from_files:
        top.sls: pillars/top.sls
        base.sls: pillars/base.sls
      state_top:
        base:
          "*":
            - k0s
